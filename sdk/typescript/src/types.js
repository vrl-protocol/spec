/**
 * TypeScript types for VRL Proof Bundles.
 * Implements the complete schema specified in VRL Spec §3 and §17.
 *
 * This is a JavaScript version for testing without TypeScript compiler.
 * The type comments are for documentation only.
 */

// Re-export as a module for testing purposes
module.exports = {};
